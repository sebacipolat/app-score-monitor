{
  "name": "app-score-monitor",
  "version": "1.0.0",
  "description": "App score monito get data from playstore and publish into a slack channel",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sebastian Cipolat",
  "license": "ISC",
  "dependencies": {
    "google-play-scraper": "^9.0.1"
  }
}
